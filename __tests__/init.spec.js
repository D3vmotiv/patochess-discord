import { createBot } from "./../src/init.js";

describe("Init functions", () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it("should return bot object", () => {
    expect(1).toBe(1);
  });
});
